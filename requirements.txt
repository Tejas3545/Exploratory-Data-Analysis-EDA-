streamlit==1.21.0
pandas
numpy
plotly
scikit-learn
scipy
openpyxl
requests
python-dotenv
# google-generativeai removed to avoid pydantic-core build (unused in repo)
pyarrow==12.0.1

# Use constraints to prefer binary wheels for any transitive deps
# (Streamlit Cloud respects pinned versions in requirements and pyproject)
